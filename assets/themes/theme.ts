import { Theme } from "ant-design-vue/lib/config-provider";

const colorState: Theme = {
    primaryColor: "#D83248",
    errorColor: "#D83248",
    warningColor: "#FF7145",
    successColor: "#51BC82",
    infoColor: "blue",
};

const theme = {
    theme: colorState,
};

export default theme;